# mock_creator

Simula o output do Creator (EN only) em JSON compatível com `schemas/content_package.schema.json`.

## Run

```bash
python generate_sample.py
```

Isso gera `mock_creator/sample_creator_output.json`.
