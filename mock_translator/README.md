# mock_translator

Simula o output do Translator (EN + PT/ES/IT) em JSON compatível com `schemas/content_package.schema.json`.

## Run

```bash
python generate_sample.py
```

Isso gera `mock_translator/sample_translator_output.json`.
